# Bu klasör: SUNUCU (server)

FastAPI uygulaması burada.

- `api.py` → tüm HTTP API (port **8008**)
- `pwa/` → eski yedek web arayüzü
- `requirements.txt` → Python paketleri

Başlat: kökte `1_SUNUCUYU_BASLAT.bat`
